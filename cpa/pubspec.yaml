name: cpa
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  file_picker: ^6.1.1
  file_upload: ^0.0.3
  flutter:
    sdk: flutter
  geolocator: ^10.1.0
  http: ^0.13.6
  intl: ^0.19.0
  path_provider: ^2.1.2
  xml: ^6.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
